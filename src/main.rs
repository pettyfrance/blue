// fn main() {
//     tracing_subscriber::fmt()
//         .with_env_filter(std::env::var("RUST_LOG").unwrap_or_else(|_| "warn".to_string()))
//         .init();
//
//     if let Err(err) = blue::cli::run(std::env::args()) {
//         eprintln!("Error: {err}");
//         std::process::exit(1);
//     }
// }

fn main() {
    // Do nothing for now
    return;
}
